setup:
  config:
    PORT: 3000
build:
  docker:
    # nginx: nginx/Dockerfile.nginx
    # process type must be web
    web: packages/server/Dockerfile.server
run:
  web: bin/start-nginx npm run start:heroku
